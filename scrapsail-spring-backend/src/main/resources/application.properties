# ScrapSail Spring Boot Application Configuration

# Server Configuration
server.port=8080

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=likeshkanna74@gmail.com
spring.mail.password=rvoueevkbdwtiizl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/scrapsail?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging Configuration
logging.level.com.scrapsail=DEBUG
logging.level.org.springframework.mail=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Application Information
spring.application.name=ScrapSail
spring.application.version=1.0.0

# Email Service Configuration
app.email.sender=likeshkanna74@gmail.com
app.email.sender.name=ScrapSail
app.otp.expiry.minutes=10